@page "/Login"
@using Pw_Frontend.Application.Services
@using PW_Frontend.Application.Dtos.AuthDto

@inject IAuthenticationService AuthenticationService
@inject NavigationManager NavigationManager
@inject IToastService ToastService
@layout BlankLayout

<div class="container card shadow rounded p-3 col-md-4">
    <h3>Login</h3>
    <EditForm Model="_loginDto">
        <div class="form-floating">
            <input type="email" class="form-control" @bind="_loginDto.Email"/>
            <label>Email:</label>
        </div>
        <div class="form-floating">
            <input type="password" class="form-control" @bind="_loginDto.Password"/>
            <label>Password:</label>
        </div>
        <div class="btn-group w-100 mt-3">
            <button class="btn btn-primary" type="submit" @onclick="LoginAsync">Login</button>
            <button class="btn btn-secondary" type="submit" @onclick="RegisterAsync">Register</button>
        </div>
    </EditForm>
</div>

@code {
    readonly LoginDto _loginDto = new();
    
    private async Task LoginAsync()
    {
        await AuthenticationService.Login(_loginDto);
        ToastService.ShowSuccess("Login successful");
        await Task.Delay(2000);
        NavigationManager.NavigateTo("/", replace: true);
    }
    
    private async Task RegisterAsync()
    {
        await AuthenticationService.Register(_loginDto);
        ToastService.ShowSuccess("Registration successful");
        NavigationManager.NavigateTo("/", replace: true);
    }
}